<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="utf-8">
<title>Campaign</title>
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<meta name="description" content="">
<meta name="author" content="OpenFund.co">

<!-- Le styles -->
<%= stylesheet_link_tag "bootstrap" %>
<%= stylesheet_link_tag "campaign" %>
<link href='http://fonts.googleapis.com/css?family=Signika+Negative' rel='stylesheet' type='text/css'>
<style>
body {
  padding-top: 60px; /* 60px to make the container go all the way to the bottom of the topbar */
}
</style>

<%= stylesheet_link_tag "bootstrap-responsive" %>

<!-- Le HTML5 shim, for IE6-8 support of HTML5 elements -->
<!--[if lt IE 9]>
      <script src="http://html5shim.googlecode.com/svn/trunk/html5.js"></script>
    <![endif]-->

</head>
<body>

       <script type="text/javascript">
            // this identifies your website in the createToken call below
            Stripe.setPublishableKey('YOUR_PUBLISHABLE_KEY');

            function stripeResponseHandler(status, response) {
                if (response.error) {
                    // re-enable the submit button
                    $('.submit-button').removeAttr("disabled");
                    // show the errors on the form
                    $(".payment-errors").html(response.error.message);
                } else {
                    var form$ = $("#payment-form");
                    // token contains id, last4, and card type
                    var token = response['id'];
                    // insert the token into the form so it gets submitted to the server
                    form$.append("<input type='hidden' name='stripeToken' value='" + token + "' />");
                    // and submit
                    form$.get(0).submit();
                }
            }

            $(document).ready(function() {
                $("#payment-form").submit(function(event) {
                    // disable the submit button to prevent repeated clicks
                    $('.submit-button').attr("disabled", "disabled");
                    // createToken returns immediately - the supplied callback submits the form if there are no errors
                    Stripe.createToken({
                        number: $('.card-number').val(),
                        cvc: $('.card-cvc').val(),
                        exp_month: $('.card-expiry-month').val(),
                        exp_year: $('.card-expiry-year').val()
                    }, stripeResponseHandler);
                    return false; // submit from callback
                });
            });

function toggle() {
  var ele = document.getElementById("meatPotatos");
  var text = document.getElementById("displayText");
  if(ele.style.display == "none") {
        ele.style.display = "block";
    text.innerHTML = "show";
    }
  else {
    ele.style.display = "none";
    text.innerHTML = "hide";
  }
} 

</script>


<!--fixed header--> <div class="navbar navbar-inverse navbar-fixed-top ">
<div class="navbar-inner ">
  <div class="span9 offset2" id="headline">
    <h1>OpenFund.co</h1>
  </div>
</div>
</div> 

<div id="title"><img src="/images/pocket-shadow.png" ></div>
<!--/fixed header--> 

<div class="hero-unit">
  <div class="container-fluid">
<div class="row-fluid">
<div class="span3 offset2" id="terms">
<div id="levels">
            <h4>Contribute 10$</h4>
               <p >lerbBlerbBlerbBlerbBlerbBlerbBlerblerbBlerbBlerbBlerbBlerbBlerbBlerbBlerblerbBlerbBlerbBlerbBlerbBlerbBBlerblerbBlerbBlerbBlerbBlerbBlerbBBlerblerbBlerbBlerbBlerbBlerbBlerbBBlerblerbBlerbBlerbBlerbBlerbBlerbB</p>              
          
          <h4>Terms:</h4>
<p>Your credit card will be bill immediately after submission. Funds are directly sent to the campaign. Campaign managers have the ability to end campaigns early. Any questions about this campaign should directed towards the campaign managers OpenFund.co does not endorse any campaigns.</p>
</div>
<!--Sidebar content-->
</div>
<div class="span6">
          <div class="container span9" >
          <!--Step1-->  <div class="row " >
              
      
                <form action="" method="POST" id="payment-form" class="form-horizontal  ">
                   <br> <div class="control-group">
                      <label class="control-label" for="inputName">Full Name</label>
                      <div class="controls">
                        <input type="text" id="inputName" placeholder="Name">
                      </div>
                    </div>
                    <div class="control-group">
                      <label class="control-label" for="inputEmail">Email</label>
                      <div class="controls">
                        <input type="email" id="inputEmail" placeholder="Email">
                      </div>
                    </div>
                   
                    <div class="control-group">
                      <div class="controls">
                      
                      </div>
                    </div>
  <!-- to display errors returned by createToken -->
  <span class="payment-errors"></span>

      <div class="control-group">
                      <label class="control-label" for="inputCCN">Card Number</label>
                      <div class="controls">
                        <input type="text" id="inputCCN" class="card-number" placeholder="CC Number">
                      </div>
                    </div>
<div class="control-group">
                      <label class="control-label" for="inputCVC">CVC</label>
                      <div class="controls">
                        <input type="text" id="inputCVC" class="card-cvc span2" placeholder="CVC">
                      </div>
                    </div>
          
     <div class="control-group">
                      <label class="control-label" for="inputMY">Expiration (MM/YYYY)</label>
                      <div class="controls">
                        <input type="text" id="inputMonth" class="card-expiry-month span2" placeholder="MM"> 
                      <span>/</span>
                        <input type="text" id="inputMonth" class="card-expiry-year span3" placeholder="YYYY">
                      </div>
                    </div>
      <button type="submit" class="btn btn-large btn-success "  id="paymentbutton"><i class="icon-heart"></i> Submit Payment</button>
  
  <p class="muted" style="text-align:right; font-size: 10px">-Powered by Stripe</p>    </form>      
</div>

</div>
<!--Body content-->
</div>
<!--end fluid--></div></div>

</div>

<div id="thankyoul"><img src="/images/pocket-shadow.png" ></div><br><!--/mainbox-->
<!--Start Sections--> 

<!--/Sections-->

<!--Start Footer-->
<div class="row">
     <div class="span10  offset1">  
<p id="footers"><i class="icon-globe"></i>Powered By: <a href="http://openfund.co">OpenFund.co</a> </p>


</div></div>